rootProject.name = "uniffi-plugin"

dependencyResolutionManagement {
    repositories {
        gradlePluginPortal()
    }
}

pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}
