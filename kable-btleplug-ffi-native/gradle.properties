# The generated bindings live in the shared `nativeMain` source set and reference the cinterop
# declarations, which are only visible from a shared source set once the commonizer has run.
kotlin.mpp.enableCInteropCommonization=true
