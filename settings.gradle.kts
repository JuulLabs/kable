pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

include(
    "exceptions",
    "core",
    "log-engine-khronicle",
    "log-engine-tuulbox",
)
