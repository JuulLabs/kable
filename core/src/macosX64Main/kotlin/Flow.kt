package com.juul.kable

//import kotlinx.coroutines.flow.MutableSharedFlow
import kotlinx.coroutines.runBlocking

//internal fun <E> MutableSharedFlow<E>.emitBlocking(element: E) {
//    // Fast path.
//    if (tryEmit(element)) return
//
//    // Slow path.
//    runBlocking { emit(element) }
//}
